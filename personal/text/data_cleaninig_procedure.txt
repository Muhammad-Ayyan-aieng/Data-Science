START clean_data()
  │
  ▼
Check if filtered_data.csv exists?
  │
  ├─── NO ──► Print error ──► RETURN None
  │
  ▼ YES
Load filtered_data.csv into DataFrame
  │
  ▼
Initialize cleaning_log dictionary
  │
  ▼
═══════════════════════════════════════
SECTION 1: MISSING VALUES
═══════════════════════════════════════
  │
  ├──► Count missing values per column
  │
  ├──► Print columns with missing values
  │
  ├──► For numerical columns:
  │       Fill missing with MEDIAN
  │
  ├──► For categorical columns:
  │       Fill missing with MODE
  │
  └──► Print total missing values after (should be 0)
  │
  ▼
═══════════════════════════════════════
SECTION 2: OUTLIERS
═══════════════════════════════════════
  │
  ├──► Temperature: Remove < -50°F or > 130°F
  │
  ├──► Humidity: Remove < 0% or > 100%
  │
  ├──► Visibility: Remove < 0 miles
  │
  └──► Print count of rows removed
  │
  ▼
═══════════════════════════════════════
SECTION 3: STANDARDIZATION
═══════════════════════════════════════
  │
  ├──► Convert Start_Time to datetime
  │
  ├──► Extract: Hour, Month, DayOfWeek
  │
  ├──► Create: IsWeekend (1/0)
  │
  ├──► Create: TimeOfDay (Morning/Afternoon/Evening/Night/Late Night)
  │
  └──► Standardize Weather_Condition to UPPERCASE (no spaces)
  │
  ▼
═══════════════════════════════════════
SECTION 4: SAVE
═══════════════════════════════════════
  │
  └──► Save to cleaned_accidents.csv
  │
  ▼
═══════════════════════════════════════
SECTION 5: SUMMARY
═══════════════════════════════════════
  │
  └──► Print cleaning statistics
  │
  ▼
RETURN cleaned DataFrame
  │
  ▼
END